# Job Board Module

This folder is a separate module boundary for job-board-specific logic.

Use it for:
- domain types
- data adapters/services
- feature utilities
- UI components (if you later choose to colocate them here)

Current starter exports are in `index.ts`.
